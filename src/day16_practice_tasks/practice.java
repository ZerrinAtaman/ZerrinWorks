package day16_practice_tasks;

import java.util.*;
import java.util.LinkedHashSet;
import java.util.Set;

public class practice {

    public static void main(String[] args) {

        /*SortedSet<String> sortedNumbers = new TreeSet<>(Arrays.asList("banana", "apple", "peace", "grape", "orange"));

        System.out.println(sortedNumbers);

        HashSet<Integer> numbers = new HashSet<>(Arrays.asList(20,10,5,10,20,30));
        System.out.println(numbers);

        Set<Integer> aaa = new LinkedHashSet<>(Arrays.asList(1,3,5,7));
        Set<Integer> bbb = new LinkedHashSet<>(Arrays.asList(2,4,6,8));

        Set<Integer> ccc = new LinkedHashSet<>(aaa);
        ccc.addAll(bbb);

        System.out.println(ccc);

        Set<Integer> no1 = new HashSet<>(Arrays.asList(1,2,3,5));
        Set<Integer> no2 = new HashSet<>(Arrays.asList(2,3,4));

        no1.removeAll(no2);

        System.out.println(no1);*/

      /*  SortedSet<Integer> number1 = new TreeSet<>(Arrays.asList(10, 8, 11, 20, 5, 0));
        SortedSet<Integer> number2 = new TreeSet<>(Arrays.asList(30, 0, 20, 40, 50, 10, 60));

        number1.retainAll(number2);

        System.out.println(number1);*/
    }
}
